piconv
======

auto detect the file encoding format and iconv it to specified encoding format


> * 1、正则匹配，获取所有匹配的文件列表；
> * 2、快速遍历多层级目录，获取文件列表；
> * 3、文件ENCODE自动识别；iconv需要手动去指定from to的encoding模式
> * 4、强大的异常处理；
> * 5、方便的调用接口；（指定src目录，缺省的dest目录，指定from-to coding，默认的匹配方式）
